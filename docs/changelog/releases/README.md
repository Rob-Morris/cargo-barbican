# Stable Releases

This folder is reserved for later stable-release notes if cargo-barbican needs
to distinguish an ordinary shipped version from a later stable promotion of that
same version.

Until then, the live history is carried by [docs/CHANGELOG.md](../../CHANGELOG.md)
and the per-version files under `docs/changelog/`.
